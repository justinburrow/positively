section
  h1 Login to your account
  .input-holder
    form
      section
        .half
          input(type="email" data-ng-model="create.email" placeholder="Email Address")
        .half
          input(type="password" data-ng-model="create.password" placeholder="Password")
      section.position-relative
        a.left-link(ui-sref="register") Don't have an account?  Create one
        .btn.btn--submit Let's get started